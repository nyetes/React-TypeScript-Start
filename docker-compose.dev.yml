version: "3.8"

services:
  app:
    container_name: react-ts-starter
    image: react-ts-starter
    build:
      context: .
      target: development
    volumes:
      - ./src:/app/src
    ports:
      - 3000:3000